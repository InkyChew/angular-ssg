import { ExtractTextPipe } from './extract-text.pipe';

describe('ExtractTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractTextPipe();
    expect(pipe).toBeTruthy();
  });
});
